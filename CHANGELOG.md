# سجل التغييرات — الورشة الفنية (Workshop Mini)

هذا الملف يجمع كل ملاحظات التحديثات اللي كانت متفرقة في أكتر من ملف (README.md, README-V2.md, README.txt, README-BATCH2-CORRECTIONS.txt, README-V11.2.1-FIXES.txt, README-V11.2.6-SIMPLE-UI.txt, UPDATE-INSTRUCTIONS.txt, workshop-mini-enhancements.md) في مكان واحد مرتب بالتاريخ/رقم الإصدار (الأحدث فوق). الترقيم القديم كان غير منتظم (V2، Batch 2، V11.x بدون تسلسل ثابت)، فبعض الدفعات القديمة اتحطت تحت "دفعات مبكرة" لعدم وجود رقم إصدار واضح لها وقت كتابتها.

---

## v11-29 — 2026-08-27 — تصحيحات: صور يتيمة، مخزون الإلغاء، الإيراد المحقق

⚠️ نفس ملحوظة الدفعات اللي قبلها: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check`
بس، من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: حذف جهاز/عميل
له صورة والتأكد إنها اتشالت (من حجم تخزين المتصفح أو أدوات المطور)، إلغاء
أمر شغل فيه قطع غيار من المخزن والتأكد إن الكمية رجعت، إعادة فتح أمر ملغي
والتأكد إن القطع اتخصمت تاني (أو ظهرت رسالة واضحة لو مش متاحة)، وفتح تقرير
الإيراد للتأكد إن قطع الغيار بقت بتظهر بس بعد قفل الأمر.

- 🐛 **حذف `app.js` (132KB) نهائيًا**: كان كود ميت 100% من مرحلة قبل تقسيم
  المشروع لملفات `app-*.js` — مش محمّل في أي صفحة HTML ولا في Service
  Worker، وفيه نسخة قديمة من نفس الدوال ممكن تلخبط أي تعديل مستقبلي لو
  اتفتح بالغلط بدل الملف الصحيح. نفس الحالة اللي اتصلحت قبل كده مع
  `app-core.js` و`twms-ui.js` في v11-18/v11-21.
- 🐛 **تنظيف صور الأجهزة اليتيمة في IndexedDB عند الحذف**: حذف جهاز مفرد
  (`deleteDeviceRecord`)، حذف عميل (بيسحب أجهزته)، `deleteAllDevices()`،
  `deleteAllCustomers()`، والمسح الشامل `deleteAllOperationalData()` كانوا
  بيمسحوا السجل من localStorage بس من غير ما يمسحوا صورة الجهاز المرتبطة
  من IndexedDB (`image-store.js`) — الصورة كانت تفضل محفوظة يتيمة للأبد
  وتاكل مساحة تخزين الجهاز بصمت. دلوقتي كل مسارات الحذف بتنضّف الصور
  المرتبطة، بنفس المنطق اللي كان مطبّق فعلاً على حذف قطعة الغيار
  (`deletePartRecord`) من الأول.
- 🐛 **إرجاع قطع الغيار للمخزن عند إلغاء أمر الشغل**: إلغاء أمر شغل (من
  التغيير السريع للحالة في القائمة، أو من صفحة التعديل الكاملة) كان بيغيّر
  الحالة لـ"ملغي" بس من غير ما يرجّع قطع الغيار المخصومة منه للمخزن —
  بعكس حذف الأمر نهائيًا اللي كان بيرجّعها صح. دلوقتي الإلغاء بيرجّع كل
  القطع المستخدمة، وإعادة فتح أمر ملغي (زي ما هو موثّق في
  `WORK_ORDER_LIFECYCLE_APPROVED.md`) بترجع تخصمها تاني لو لسه متاحة بنفس
  الكمية، وإلا بترفض إعادة الفتح برسالة واضحة بدل ما تفتح بمخزون غير متسق.
- 🏗️ **الإيراد المحقق: قطع الغيار بقت بتُحسب للأوامر المقفولة نهائيًا بس**،
  بنفس منطق المصنعية تمامًا. قبل كده كانت المصنعية بتتحسب من الأوامر
  المقفولة بس، لكن قيمة قطع الغيار وتكلفتها كانت بتتحسب من كل أوامر الفترة
  بغض النظر عن حالة الإغلاق — يعني أمر لسه "جاري التنفيذ" كانت قطعه بتدخل
  في "الإيراد" و"المكسب" وهو لسه ماتقفلش. اتصلح في `financeReport()`
  و`reportRowMeta()` (`app-dashboard-reports.js`) وتقرير الطباعة الكامل
  (`reports.js`) مع بعض عشان يفضلوا متطابقين.
- رفع رقم الكاش في Service Worker (`workshop-v11-29-fixes`) عشان الأجهزة
  اللي عندها نسخة مثبتة تاخد كل التصحيحات دي.

---

## v11-30 — 2026-08-27 — استكمال المراجعة: Restore، إيراد الشاشة الرئيسية، توحيد التاريخ، تنظيف التوثيق

⚠️ نفس ملحوظة v11-29: فحص كودي/منطقي + `node --check` بس. لازم تُختبر يدويًا:
استرجاع نسخة احتياطية قديمة على جهاز فيه صور مختلفة والتأكد إن الصور
القديمة اتشالت وبقيت صور الملف بس، ومراجعة رقم "إيراد الشهر" في الشاشة
الرئيسية مقابل تقرير الشهر نفسه للتأكد إنهم متطابقين.

- 🐛 **تنظيف IndexedDB قبل استرجاع نسخة احتياطية**: `restoreBackupFile`
  كان بيستورد صور الملف بـ `ImageStore.importAll` من غير ما يمسح صور
  الجهاز الحالية الأول — يعني صور من قبل الاسترجاع (بمراجع مختلفة عن
  اللي في الملف) كانت بتفضل موجودة يتيمة جنب صور النسخة المستوردة، رغم
  إن رسالة التأكيد بتقول إن الاسترجاع "هيستبدل كل بيانات النظام الحالية".
  دلوقتي بيتعمل `ImageStore.clearAll()` قبل الاستيراد مباشرة.
- 🐛 **نفس ثغرة "الإيراد المحقق" كانت موجودة في الشاشة الرئيسية**: بند
  "💰 إيراد الشهر" في `renderDash()` كان بيحسب قطع الغيار من كل أوامر
  الشهر بغض النظر عن الإغلاق (نفس الثغرة اللي اتصلحت في صفحة التقارير
  في v11-29، لكنها فاتت وقتها من `renderDash` تحديدًا). دلوقتي بيتحسب من
  الأوامر المقفولة بس، ومطابق تمامًا لرقم صفحة التقارير لنفس الشهر.
- 🏗️ **توحيد 3 نسخ مكررة من نفس منطق "تاريخ اليوم المحلي"**:
  `localDateKey` (`app-shared.js`)، `dayKeyLocal` (`app-shared.js` برضه)،
  ودالة محلية باسم `dateKey` جوه `reports.js` — التلاتة بيعملوا نفس
  الحساب بالظبط. بقى `dayKeyLocal` هو المصدر الوحيد (الأكثر أمانًا لأنه
  بيتعامل مع أي مدخل ومعاه فحص NaN)، و`localDateKey` و`dateKey` بقوا
  مجرد أسماء بديلة (alias) ليه بدون أي تغيير في أي استدعاء حالي.
- 🐛 **حذف ملفات README/توثيق قديمة** كان محتواها مدموج بالفعل في
  `CHANGELOG.md` من دفعة سابقة لكن الملفات الفعلية فضلت موجودة:
  `README-BATCH2-CORRECTIONS.txt`، `README-V11.2.1-FIXES.txt`،
  `README-V11.2.6-SIMPLE-UI.txt`، `README-V2.md`، `README.txt`،
  `UPDATE-INSTRUCTIONS.txt`، `workshop-mini-enhancements.md`. اتأكد قبل
  الحذف إن مفيش أي كود بيشاور عليهم. `README.md` (الحالي) و
  `WORK_ORDER_LIFECYCLE_APPROVED.md` (مرجع معتمد يُستخدم فعليًا في
  `migrations.js`) فضلوا زي ما هم.
- رفع رقم الكاش في Service Worker (`workshop-v11-30-fixes2`).

---

## v11-22 — 2026-08-26 — فصل app.js تدريجيًا: الخزنة والمهام في ملفات مستقلة

⚠️ نفس ملحوظة v11-21: اتنفذ بمساعدة Claude وتحقق منطقي/`node --check` بس،
من غير تشغيل فعلي في متصفح. لازم تتجرب يدويًا قبل النشر: فتح صفحة الخزنة
(إضافة حركة وارد/صرف، حفظ رصيد افتتاحي)، وصفحة المهام (إضافة/تعديل/حذف
مهمة)، وحفظ أمر شغل بعربون (بينادي `syncTreasuryForOrderDeposit`) للتأكد
إن التنقّل بين الملفات مأثرش على السلوك.

- 🏗️ **قسم الخزنة اتنقل بالكامل من `app.js` لملف `treasury.js` جديد** (13 دالة: `treasuryEntries`, `treasuryBalance`, `upsertTreasuryEntry`, `removeTreasuryEntry`, `syncTreasuryForOrderDeposit`, `syncTreasuryForOrderClose`, `syncTreasuryForExpense`, `addTreasuryManual`, `saveOpeningBalance`, `editTreasuryEntry`, `deleteTreasuryEntry`, `operationalTreasurySummary`, `renderTreasury`) — نفس الكود حرفيًا، نقل مكان بس.
- 🏗️ **قسم المهام والمتابعة اتنقل بالكامل لملف `tasks.js` جديد** (`taskRows`, `taskDateTime`, `taskPriorityClass`, `saveTask`, `clearTaskForm`, `toggleTask`, `deleteTask`, `editTask`, `renderTasks`, `initTasks`, `fillTaskRequests`) — نفس الكود حرفيًا برضه.
- 📉 **`app.js` نزل من 810 لـ 638 سطر** في الدفعتين v11-21 وv11-22 مع بعض (كانت أكبر قبل كده).
- الصفحات الخمستاشر و`service-worker.js` اتحدثوا يحمّلوا `treasury.js` و`tasks.js` قبل `app.js` (اسم الـ cache اتغيّر تاني عشان الإصدار الجديد ينزل).
- لم يتغيّر أي منطق أو سلوك ظاهر — نقل كود بس، مفيش أي تعديل في محتوى الدوال.

## v11-21 — 2026-08-26 — نسخة بيانات (schema version) + نقل الصور لـ IndexedDB + طبقة rollback + تنظيف ملفات ميتة (باقية من v11-18)

⚠️ **الدفعة دي اتنفذت بمساعدة Claude من غير تشغيل فعلي في متصفح حقيقي** (فحص
كودي/منطقي + `node --check` للتأكد من عدم وجود أخطاء syntax بس). لازم تتجرب
يدويًا قبل ما تتنشر: إضافة/تعديل جهاز وقطعة بصورة، فتح ملف الجهاز/القطعة
360° للتأكد إن الصورة بتظهر، تصدير نسخة احتياطية واسترجاعها، وفتح صفحة فيها
بيانات قديمة (قبل التحديث) للتأكد إن الترحيل شغال صح.

- 🐛 **تنظيف فعلي أخيرًا لملفات كان اتوثّق حذفها في v11-18 لكنها فضلت موجودة**: `app-core.js` (كان غير محمّل في أي صفحة أصلًا — كود ميت 100%)، `workshop-mini-classifications.js`، `twms-ui.js`، وصور الأيقونات المكررة القديمة (`icon-192.png`, `icon-192-v2.png`, `icon-192-v11-2-7.png`, `icon-512.png`, `icon-512-v2.png`, `icon-512-v11-2-7.png`). اتأكد قبل الحذف إنها مش متسجلة في أي `<script>`/`<link>` ولا في `service-worker.js` ولا `manifest.json`.
- 🏗️ **`wf_schema_version` في `shared-data.js`**: رقم إصدار لشكل البيانات المحفوظة، منفصل عن رقم إصدار التطبيق. بيانات قديمة من غير الرقم ده تتعتبر إصدار 1 ضمنيًا.
- 🏗️ **ملف جديد `image-store.js`**: تخزين الصور في IndexedDB (`workshopImagesDB`) بدل base64 جوه سجل الجهاز/القطعة في localStorage. سجل الجهاز/القطعة بقى بيحمل مرجع (id) بس. توافق كامل مع البيانات القديمة: أي قيمة `photo` لسه بادئة بـ`data:` بترجع زي ما هي من غير أي كسر.
- 🏗️ **ملف جديد `migrations.js`**: بيشغّل ترحيل 1→2 مرة واحدة بس (نقل أي صورة base64 قديمة في `wf_d`/`wf_p` لـ IndexedDB واستبدالها بمرجع)، بينادَى ويتنتظر (`await`) في أول حاجة في `DOMContentLoaded` قبل أي رندر.
- 🏗️ **`withRollback(keys, fn)` في `shared-data.js`**: تعميم نمط الـ backup/restore اليدوي اللي كان موجود في `persistRequestRecord` (باستخدام `JSON.stringify` وإرجاعه لو فشل) في مكان واحد، وتطبيقه على `wf_p` و`wf_m` مع بعض بدل `wf_p` بس.
- 🏗️ **الباك أب/الاسترجاع (`backupAllData`/`restoreBackupFile`) بقوا بيشملوا الصور** (من IndexedDB) ورقم `schemaVersion`، مع فضل الملف الناتج JSON واحد قائم بذاته زي ما كان قبل كده.
- 🏗️ صفحات HTML الخمستاشر اللي بتحمّل `app.js` بقت بتحمّل `image-store.js` و`migrations.js` قبله، و`service-worker.js` بقى بيعمل precache لهم (اتغيّر اسم الـ cache عشان يتنزل الإصدار الجديد).
- لم يتغيّر أي شكل بيانات ظاهر للمستخدم ولا سلوك واجهة في أي شاشة تانية غير المذكور فوق.

## v11-18 — 2026-08-26 — تنظيف الكود الميت (تنفيذ فعلي) + توحيد طبقة البيانات

هذه الدفعة نفّذت فعليًا التنظيف الموثّق في الدفعة اللي قبلها (كان اتكتب إنه تم لكن الملفات فضلت موجودة فعليًا في آخر نسخة مرفوعة)، بالإضافة لخطوة معمارية جديدة:

- ✅ حذف فعلي لـ `app-core.js`، `workshop-mini-classifications.js`، `twms-ui.js` (كانت لسه موجودة رغم توثيق حذفها) وصور الأيقونات المكررة (`icon-192.png`, `icon-192-v2.png`, `icon-192-v11-2-7.png`, `icon-512.png`, `icon-512-v2.png`, `icon-512-v11-2-7.png`). الحجم رجع من ~2.1MB لـ ~660KB.
- 🐛 حذف قائمة "🗺️ كل المراكز" (`customerCenterFilter` / `deviceCenterFilter`) من `customers.html` و`devices.html` — كانت موجودة بصريًا لكن بدون أي تأثير فعلي، لأن نسخة العرض النهائية (`workshop-mini-simple-ui.js`) بتستبدل دوال العرض بالكامل ومبتقراش الفلتر ده. كانت بتظهر للمستخدم إنها شغالة وهي مش شغالة.
- 🏗️ **ملف جديد `shared-data.js`**: مصدر واحد لكل الدوال المشتركة (`K`, `get`, `put`, `arr`, `esc`, `id`, `settings`, `duplicateCustomerByPhone`, `customerName`, `deviceName`, `addresses`, `addressText`) بدل ما تكون معرّفة بنفس المنطق في 3 ملفات مختلفة (`app.js`, `workshop-mini-enhancements.js`, `workshop-mini-simple-ui.js`). لازم يتحمّل قبل أي ملف تاني — مضاف لكل صفحات HTML وموجود في قائمة تخزين الـ Service Worker.
- 🏗️ **`window.defineOverride(name, sourceFile, fn)`**: أداة بسيطة بتوثّق أي مرة دالة `render*` بتتبدّل من ملف تاني (نفس السلوك بالظبط، بس بقى موثّق في الكونسول بدل ما يحصل بصمت). اتطبّقت على `renderCustomers`, `renderDevices`, `renderRequests`, `customerProfile`, `deleteCustomerRecord`, `deleteDeviceRecord`.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.

## v11-19 — 2026-08-26 — فصل منطق الحفظ عن الـ DOM + توحيد تحديث الشاشات

- 🏗️ **`saveRequest()` (حفظ أمر الشغل) اتقسمت لـ 3 دوال**: `collectRequestFormData()` (قراءة قيم الفورم فقط)، `persistRequestRecord()` (منطق الحفظ والمخزون الصِرف — بدون أي لمسة DOM، وبيرجع `{ok, request}` أو `{ok:false, error}`)، و`saveRequest()` كدالة تحكم رفيعة بتربط بينهم. نفس ترتيب العمليات، نفس الحسابات، نفس رسائل الخطأ بالظبط — اتأكدت بمحاكاة تشغيل فعلية (أمر جديد، تعديل بكمية مخزون غير متاحة مع التراجع، تعديل ناجح بيقلل الكمية المستخدمة) وطابقت النتائج المتوقعة تمامًا.
- 🏗️ **`refreshAllScreens()` في `shared-data.js`**: نقطة واحدة لتحديث كل الشاشات (`renderCustomers`, `renderDevices`, `renderRequests`, `renderDash`, `monthReport`) بعد حذف عميل أو جهاز، بدل ما تتكرر يدويًا (بترتيب مختلف كل مرة) في أكتر من مكان في `workshop-mini-enhancements.js`.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.

## v11-20 — 2026-08-26 — نفس فصل الحفظ عن الـ DOM لباقي النماذج

استكمال لنفس النمط اللي اتطبّق على أمر الشغل في الدفعة اللي قبلها، اتطبّق على باقي شاشات الحفظ:

- 🏗️ **حفظ العميل**: `collectCustomerFormData()` (قراءة الاسم/الهاتف/العنوانين) + `persistCustomerRecord()` (حفظ صِرف). تأكيد "رقم الهاتف مكرر" (`confirm()`) فضل في المعالج نفسه لأنه تفاعل مباشر مع المستخدم مش منطق بيانات.
- 🏗️ **حفظ الجهاز**: `collectDeviceFormData()` (بما فيها تحويل الصورة) + `persistDeviceRecord()` (بترجع `{ok, device}`).
- 🏗️ **حفظ القطعة**: `collectPartFormData()` + `persistPartRecord()` بنفس الشكل.
- كل الدوال الجديدة اتفحصت بمحاكاة تشغيل فعلية: إنشاء عميل/جهاز/قطعة، تعديل سجل موجود، وحالة امتلاء مساحة التخزين (تفشل بهدوء بنفس رسالة التنبيه القديمة بالظبط من غير ما تنتقل الصفحة). النتائج طابقت السلوك الأصلي تمامًا.
- لم يتغيّر أي منطق حسابات أو شكل بيانات أو سلوك ظاهر للمستخدم في هذه الدفعة.


## أحدث تعديلات — 2026-08-26

**تحسين شكل "خط السير القادم" + إعدادات المظهر**
- إعادة تصميم بطاقة "📅 خط السير القادم" في الشاشة الرئيسية بشكل بطاقة مستقلة (بدل الأسلوب القديم).
- إصلاح تعارض كان موجود بين class باسم `route-summary`/`route-group-title` مستخدم في حاجتين مختلفتين (صفحة `route.html` القديمة، وودجت الشاشة الرئيسية) — بقى لكل واحدة أسماء classes مستقلة (`rp-*`).
- إضافة إعداد جديد في صفحة الإعدادات: مظهر خط السير القادم (تلقائي حسب النظام / داكن / فاتح / لون مخصص).

**إصلاحات تشغيل يومي (Service Worker / PWA)**
- إصلاح مرجعين لأيقونة إشعارات قديمة (`icon-192-v11-2-7.png`) كانوا لسه موجودين في `service-worker.js` و`app.js` بعد ما باقي المشروع اتحدّث لأيقونة أحدث — بقوا كلهم يشاورو على نفس الأيقونة الحالية.
- إضافة `workshop-mini-simple-ui.js` و`workshop-mini-enhancements.js` لقائمة الملفات المحفوظة أوفلاين (Service Worker) — كانوا ناقصين مع إنهم أساسيين لصفحات أوامر الشغل/العملاء/الأجهزة.
- رفع رقم نسخة الكاش عشان الأجهزة اللي عندها نسخة مثبتة تاخد التحديثات دي.

**تنظيف الكود**
- حذف ملفات كانت غير مستخدمة خالص في أي صفحة: `app-core.js` (نسخة Core بديلة قديمة)، `workshop-mini-classifications.js`، `twms-ui.js`.
- حذف نسخ أيقونات مكررة بايت-لبايت (كان فيه 3 نسخ متطابقة من نفس أيقونة 192px، و3 نسخ من أيقونة 512px) وإبقاء أحدث نسخة بس من كل مقاس.
- حجم المشروع نزل من ~2.1MB إلى ~644KB.
- دمج كل ملفات التوثيق المتفرقة في هذا الملف.

---

## V11.11
- قطعة غيار خارج المخزن: تضاف داخل أمر الشغل (اسم + سعر شراء + سعر بيع) ومالهاش أي علاقة بالمخزن أو كمياته أو أرقام قطع الغيار في التقارير. مكسبها فقط (سعر البيع − سعر الشراء) بيتضاف تلقائيًا لبند "المصنعية" وفي كل حسابات الإيراد والمكسب.
- تنسيق كامل لصفحة التقارير والتحليلات (بطاقات، جداول، قوائم) — كانت من غير أي CSS، بقت متناسقة ومتجاوبة على الموبايل ومهيأة للطباعة.
- تنسيق الأرقام المالية في التقارير بفواصل الآلاف لسهولة القراءة.
- رفع رقم الكاش في Service Worker.

## V11.10 — واجهة الطباعة والمشاركة
- إزالة أزرار الطباعة والمشاركة العامة من الصفحات.
- إضافة أزرار صغيرة داخل سياق العميل والجهاز وأمر الشغل وقطعة المخزن فقط.
- إضافة مهام اليوم إلى إحصائيات الصفحة الرئيسية بدل بطاقة مستقلة.
- تقرير الورشة يعتمد على الحالة الحالية للأجهزة/الأوامر داخل الورشة.

## PWA ICON UPDATE V11.2.7
- رفع رقم نسخة الـ manifest والـ Service Worker cache.
- استخدام أسماء ملفات أيقونة جديدة فريدة عشان أندرويد ميحتفظش بالأيقونة القديمة القديمة في الكاش.
- بيانات/تخزين التطبيق ماتأثرتش بتحديث الأيقونة ده.

## V11.2.6 — Simple UI
- تبسيط واجهة أوامر الشغل وإزالة تكرار بطاقات الأوامر.
- ملخص واحد لأوامر اليوم، الورشة، المكتملة، انتظار قطع الغيار، والمتأخر.
- زر «كل الأوامر» لعرض القائمة التفصيلية عند الطلب فقط.
- العملاء والأجهزة والمخزن يبدأون بملخص مختصر، والقائمة الكاملة تظهر بزر «كل ...».
- خط السير القادم يجمع الأوامر حسب التاريخ ثم المركز ثم القرية.
- المراكز والقرى تعتمد على الإعدادات الحالية، ويمكن إضافة قرى لاحقًا من غير تعديل كود.
- عند إغلاق أمر الشغل يتم تسجيله كمغلق مع تثبيت الحالة «مكتمل» وتاريخ الإغلاق.
- لا يتم تغيير بنية localStorage أو حذف البيانات الحالية.

### BATCH 2.6 HOTFIX — 2026-08-21
- توحيد استرجاع أوامر الشغل من المفاتيح القديمة/الأساسية من غير استبدال البيانات الموجودة.
- إضافة زر إغلاق مباشر للأمر المكتمل من قائمة أوامر الشغل.
- تحسين إغلاق الأمر وتحديث القائمة مباشرة.
- إصلاح توقف صفحة أوامر الشغل عن عرض القائمة بسبب اعتماد على عناصر DOM غير معرفة صراحة (`requestSearch`/`statusFilter`/`workshopFilter`) — اتربطت مباشرة عبر `getElementById`.
- تحديث نسخة Service Worker.

## V11.2.3
- تحديث ملفات ثابتة أونلاين أولًا (online-first) مع fallback أوفلاين ونسخة كاش جديدة.

## V11.2.1 Patch
- إصلاح فتح ملفات 360° أوفلاين (`customer.html?id=`, `device.html?id=`, `request.html?id=`, `part.html?id=`) بالتعامل مع الصفحة حسب المسار من غير احتساب query string، مع بقاء الـ ID في الرابط ليقرأه `app.js`.
- إصلاح أماكن أزرار "فتح 360°" و"حذف" في كروت العملاء والأجهزة وأوامر الشغل — بقت في عمود مستقل أقصى اليسار من غير تداخل مع البيانات.
- رفع رقم Cache للـ Service Worker.
- فحص syntax لملفات JavaScript وHTML بنجاح.

## V11.2
- تحديث أيقونات PWA/التطبيق باستخدام لوجو الورشة؛ منطق التطبيق لم يتغير.

## V11.1
- Manifest للتثبيت كتطبيق على الهاتف.
- Service Worker للتشغيل Offline بعد أول تحميل ناجح.
- Cache لملفات النظام الأساسية.
- تحديث ملفات الواجهة عند توفر الإنترنت من غير لمس بيانات localStorage.
- زر تثبيت التطبيق يظهر عندما يسمح المتصفح بذلك.

---

## دفعات مبكرة (بدون رقم إصدار ثابت وقتها)

### تحديث المهام والخزنة (الدفعة 01)
الملفات المتأثرة وقتها: `app.js`, `index.html`, `treasury.html`, `service-worker.js`, `tasks.html`.
- إضافة قسم المهام والمتابعة.
- فصل الخزنة عن تحصيلات أوامر الشغل ومصاريف التشغيل.
- الخزنة بقت درج نقدي مستقل: رصيد افتتاحي + وارد + صرف + سبب + لمن/من + المكان + تفاصيل.
- عرض ملخص حسابات التشغيل داخل الخزنة للمتابعة فقط، من غير إدخالها في رصيد الدرج.
- تحديث Service Worker ليكاش صفحة المهام.

### Workshop Mini V2 — Focused Enhancements
نطاق التعديل: العملاء والأجهزة فقط، بدون أقسام جديدة.
- تحسين بحث العملاء (الاسم، الهاتف، العنوانين).
- ملف العميل 360°: ملخص أوضح + الأجهزة + أوامر الشغل المرتبطة.
- حذف العميل بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- حذف الجهاز بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- تحسين بحث الأجهزة (العميل، النوع، التصنيف، الماركة، الموديل، الوصف).
- إصلاح helper مفقود خاص باكتشاف رقم هاتف عميل مكرر، مع إبقاء قرار إنشاء العميل للمستخدم.
- لا توجد إضافات تخص الفنيين أو Backend أو الولاء أو الإشعارات أو أي قسم جديد.

> ملحوظة: كانت هذه الدفعة مبنية على مواصفة تفصيلية طويلة (`workshop-mini-enhancements.md`) بخطوط عريضة زي: الحفاظ على 4 أقسام بس (عملاء/أجهزة/أوامر شغل/مخزن)، تحسين الترابط بينهم وصحة الحسابات وقطع الغيار، تحسين رسائل النجاح/الخطأ والتأكيد قبل الحذف، وعدم إضافة فنيين/بوابة عميل/ولاء/Backend في هذه المرحلة. أغلب بنودها اتنفذت وبقت جزء من النظام الحالي، فتم الاكتفاء بالملخص ده بدل نقل المواصفة كاملة.

### Batch 2 corrections
- الكاميرا بقت تلتقط صورة واحدة في كل ضغطة وتتجمع الصور من كذا ضغطة كاميرا.
- استبدال حقول اختيار العميل/الجهاز/العنوان المكررة الشكل بمُنتقي واحد قابل للبحث لكل واحد.
- بنية البيانات ومفاتيح localStorage لم تتغير.
- ملحوظة: النسخة لسه بتخزن بيانات الصور في تخزين المتصفح؛ تخزين دائم على سيرفر يحتاج Backend/Object Storage منفصل، ومش متوفر في هذه المرحلة.

### إضافة مجلدات أوامر الشغل (تجربة قديمة — تم تجاوزها)
كانت هذه الدفعة بتضيف تصنيف أوامر الشغل في 4 مجلدات (مكتملة/الورشة/اليوم/انتظار قطع غيار)، عن طريق استبدال `requests.html` وملف Core منفصل باسم `app-core.js`.
> ملحوظة: `app-core.js` كان نظام Core بديل بمفاتيح تخزين مختلفة تمامًا عن اللي شغال دلوقتي، ولم يكن مستخدمًا فعليًا في أي صفحة HTML بالمشروع الحالي. تم حذفه بتاريخ 2026-08-26 ضمن تنظيف الكود الميت (نفس فكرة "المجلدات" اتحققت لاحقًا بشكل مختلف عبر ملخصات أوامر الشغل في V11.2.6).
