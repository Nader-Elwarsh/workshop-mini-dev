# سجل التغييرات — الورشة الفنية (Workshop Mini)

هذا الملف يجمع كل ملاحظات التحديثات اللي كانت متفرقة في أكتر من ملف (README.md, README-V2.md, README.txt, README-BATCH2-CORRECTIONS.txt, README-V11.2.1-FIXES.txt, README-V11.2.6-SIMPLE-UI.txt, UPDATE-INSTRUCTIONS.txt, workshop-mini-enhancements.md) في مكان واحد مرتب بالتاريخ/رقم الإصدار (الأحدث فوق). الترقيم القديم كان غير منتظم (V2، Batch 2، V11.x بدون تسلسل ثابت)، فبعض الدفعات القديمة اتحطت تحت "دفعات مبكرة" لعدم وجود رقم إصدار واضح لها وقت كتابتها.

---

## أحدث تعديلات — 2026-08-26

**تحسين شكل "خط السير القادم" + إعدادات المظهر**
- إعادة تصميم بطاقة "📅 خط السير القادم" في الشاشة الرئيسية بشكل بطاقة مستقلة (بدل الأسلوب القديم).
- إصلاح تعارض كان موجود بين class باسم `route-summary`/`route-group-title` مستخدم في حاجتين مختلفتين (صفحة `route.html` القديمة، وودجت الشاشة الرئيسية) — بقى لكل واحدة أسماء classes مستقلة (`rp-*`).
- إضافة إعداد جديد في صفحة الإعدادات: مظهر خط السير القادم (تلقائي حسب النظام / داكن / فاتح / لون مخصص).

**إصلاحات تشغيل يومي (Service Worker / PWA)**
- إصلاح مرجعين لأيقونة إشعارات قديمة (`icon-192-v11-2-7.png`) كانوا لسه موجودين في `service-worker.js` و`app.js` بعد ما باقي المشروع اتحدّث لأيقونة أحدث — بقوا كلهم يشاورو على نفس الأيقونة الحالية.
- إضافة `workshop-mini-simple-ui.js` و`workshop-mini-enhancements.js` لقائمة الملفات المحفوظة أوفلاين (Service Worker) — كانوا ناقصين مع إنهم أساسيين لصفحات أوامر الشغل/العملاء/الأجهزة.
- رفع رقم نسخة الكاش عشان الأجهزة اللي عندها نسخة مثبتة تاخد التحديثات دي.

**تنظيف الكود**
- حذف ملفات كانت غير مستخدمة خالص في أي صفحة: `app-core.js` (نسخة Core بديلة قديمة)، `workshop-mini-classifications.js`، `twms-ui.js`.
- حذف نسخ أيقونات مكررة بايت-لبايت (كان فيه 3 نسخ متطابقة من نفس أيقونة 192px، و3 نسخ من أيقونة 512px) وإبقاء أحدث نسخة بس من كل مقاس.
- حجم المشروع نزل من ~2.1MB إلى ~644KB.
- دمج كل ملفات التوثيق المتفرقة في هذا الملف.

---

## V11.11
- قطعة غيار خارج المخزن: تضاف داخل أمر الشغل (اسم + سعر شراء + سعر بيع) ومالهاش أي علاقة بالمخزن أو كمياته أو أرقام قطع الغيار في التقارير. مكسبها فقط (سعر البيع − سعر الشراء) بيتضاف تلقائيًا لبند "المصنعية" وفي كل حسابات الإيراد والمكسب.
- تنسيق كامل لصفحة التقارير والتحليلات (بطاقات، جداول، قوائم) — كانت من غير أي CSS، بقت متناسقة ومتجاوبة على الموبايل ومهيأة للطباعة.
- تنسيق الأرقام المالية في التقارير بفواصل الآلاف لسهولة القراءة.
- رفع رقم الكاش في Service Worker.

## V11.10 — واجهة الطباعة والمشاركة
- إزالة أزرار الطباعة والمشاركة العامة من الصفحات.
- إضافة أزرار صغيرة داخل سياق العميل والجهاز وأمر الشغل وقطعة المخزن فقط.
- إضافة مهام اليوم إلى إحصائيات الصفحة الرئيسية بدل بطاقة مستقلة.
- تقرير الورشة يعتمد على الحالة الحالية للأجهزة/الأوامر داخل الورشة.

## PWA ICON UPDATE V11.2.7
- رفع رقم نسخة الـ manifest والـ Service Worker cache.
- استخدام أسماء ملفات أيقونة جديدة فريدة عشان أندرويد ميحتفظش بالأيقونة القديمة القديمة في الكاش.
- بيانات/تخزين التطبيق ماتأثرتش بتحديث الأيقونة ده.

## V11.2.6 — Simple UI
- تبسيط واجهة أوامر الشغل وإزالة تكرار بطاقات الأوامر.
- ملخص واحد لأوامر اليوم، الورشة، المكتملة، انتظار قطع الغيار، والمتأخر.
- زر «كل الأوامر» لعرض القائمة التفصيلية عند الطلب فقط.
- العملاء والأجهزة والمخزن يبدأون بملخص مختصر، والقائمة الكاملة تظهر بزر «كل ...».
- خط السير القادم يجمع الأوامر حسب التاريخ ثم المركز ثم القرية.
- المراكز والقرى تعتمد على الإعدادات الحالية، ويمكن إضافة قرى لاحقًا من غير تعديل كود.
- عند إغلاق أمر الشغل يتم تسجيله كمغلق مع تثبيت الحالة «مكتمل» وتاريخ الإغلاق.
- لا يتم تغيير بنية localStorage أو حذف البيانات الحالية.

### BATCH 2.6 HOTFIX — 2026-08-21
- توحيد استرجاع أوامر الشغل من المفاتيح القديمة/الأساسية من غير استبدال البيانات الموجودة.
- إضافة زر إغلاق مباشر للأمر المكتمل من قائمة أوامر الشغل.
- تحسين إغلاق الأمر وتحديث القائمة مباشرة.
- إصلاح توقف صفحة أوامر الشغل عن عرض القائمة بسبب اعتماد على عناصر DOM غير معرفة صراحة (`requestSearch`/`statusFilter`/`workshopFilter`) — اتربطت مباشرة عبر `getElementById`.
- تحديث نسخة Service Worker.

## V11.2.3
- تحديث ملفات ثابتة أونلاين أولًا (online-first) مع fallback أوفلاين ونسخة كاش جديدة.

## V11.2.1 Patch
- إصلاح فتح ملفات 360° أوفلاين (`customer.html?id=`, `device.html?id=`, `request.html?id=`, `part.html?id=`) بالتعامل مع الصفحة حسب المسار من غير احتساب query string، مع بقاء الـ ID في الرابط ليقرأه `app.js`.
- إصلاح أماكن أزرار "فتح 360°" و"حذف" في كروت العملاء والأجهزة وأوامر الشغل — بقت في عمود مستقل أقصى اليسار من غير تداخل مع البيانات.
- رفع رقم Cache للـ Service Worker.
- فحص syntax لملفات JavaScript وHTML بنجاح.

## V11.2
- تحديث أيقونات PWA/التطبيق باستخدام لوجو الورشة؛ منطق التطبيق لم يتغير.

## V11.1
- Manifest للتثبيت كتطبيق على الهاتف.
- Service Worker للتشغيل Offline بعد أول تحميل ناجح.
- Cache لملفات النظام الأساسية.
- تحديث ملفات الواجهة عند توفر الإنترنت من غير لمس بيانات localStorage.
- زر تثبيت التطبيق يظهر عندما يسمح المتصفح بذلك.

---

## دفعات مبكرة (بدون رقم إصدار ثابت وقتها)

### تحديث المهام والخزنة (الدفعة 01)
الملفات المتأثرة وقتها: `app.js`, `index.html`, `treasury.html`, `service-worker.js`, `tasks.html`.
- إضافة قسم المهام والمتابعة.
- فصل الخزنة عن تحصيلات أوامر الشغل ومصاريف التشغيل.
- الخزنة بقت درج نقدي مستقل: رصيد افتتاحي + وارد + صرف + سبب + لمن/من + المكان + تفاصيل.
- عرض ملخص حسابات التشغيل داخل الخزنة للمتابعة فقط، من غير إدخالها في رصيد الدرج.
- تحديث Service Worker ليكاش صفحة المهام.

### Workshop Mini V2 — Focused Enhancements
نطاق التعديل: العملاء والأجهزة فقط، بدون أقسام جديدة.
- تحسين بحث العملاء (الاسم، الهاتف، العنوانين).
- ملف العميل 360°: ملخص أوضح + الأجهزة + أوامر الشغل المرتبطة.
- حذف العميل بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- حذف الجهاز بقى متاح مع تحذير وتأكيد واضح، وإعادة قطع الغيار للمخزن.
- تحسين بحث الأجهزة (العميل، النوع، التصنيف، الماركة، الموديل، الوصف).
- إصلاح helper مفقود خاص باكتشاف رقم هاتف عميل مكرر، مع إبقاء قرار إنشاء العميل للمستخدم.
- لا توجد إضافات تخص الفنيين أو Backend أو الولاء أو الإشعارات أو أي قسم جديد.

> ملحوظة: كانت هذه الدفعة مبنية على مواصفة تفصيلية طويلة (`workshop-mini-enhancements.md`) بخطوط عريضة زي: الحفاظ على 4 أقسام بس (عملاء/أجهزة/أوامر شغل/مخزن)، تحسين الترابط بينهم وصحة الحسابات وقطع الغيار، تحسين رسائل النجاح/الخطأ والتأكيد قبل الحذف، وعدم إضافة فنيين/بوابة عميل/ولاء/Backend في هذه المرحلة. أغلب بنودها اتنفذت وبقت جزء من النظام الحالي، فتم الاكتفاء بالملخص ده بدل نقل المواصفة كاملة.

### Batch 2 corrections
- الكاميرا بقت تلتقط صورة واحدة في كل ضغطة وتتجمع الصور من كذا ضغطة كاميرا.
- استبدال حقول اختيار العميل/الجهاز/العنوان المكررة الشكل بمُنتقي واحد قابل للبحث لكل واحد.
- بنية البيانات ومفاتيح localStorage لم تتغير.
- ملحوظة: النسخة لسه بتخزن بيانات الصور في تخزين المتصفح؛ تخزين دائم على سيرفر يحتاج Backend/Object Storage منفصل، ومش متوفر في هذه المرحلة.

### إضافة مجلدات أوامر الشغل (تجربة قديمة — تم تجاوزها)
كانت هذه الدفعة بتضيف تصنيف أوامر الشغل في 4 مجلدات (مكتملة/الورشة/اليوم/انتظار قطع غيار)، عن طريق استبدال `requests.html` وملف Core منفصل باسم `app-core.js`.
> ملحوظة: `app-core.js` كان نظام Core بديل بمفاتيح تخزين مختلفة تمامًا عن اللي شغال دلوقتي، ولم يكن مستخدمًا فعليًا في أي صفحة HTML بالمشروع الحالي. تم حذفه بتاريخ 2026-08-26 ضمن تنظيف الكود الميت (نفس فكرة "المجلدات" اتحققت لاحقًا بشكل مختلف عبر ملخصات أوامر الشغل في V11.2.6).
